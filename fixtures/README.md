# Fixtures

Hand-written pastes used as conformance cases. They **name** game entities and nothing else
(content boundary, CLAUDE.md). `vice` and `shigen` are Francesco's two real builds (D17); the
compiled sheets they mirror live outside this repo.

Known fidelity notes:
- `vice`: the paper sheet lists three invocations at level 1; a 2024 Warlock has one at level 1
  and three at level 2, so the paste places two of them at `L2`. Skills chosen at Warlock 1 are
  not recorded on the compiled sheet, so the line is omitted (D13). Specialized Design's two
  tools are a species pick and live in the `Species` details (D22).
- `shigen`: the Human origin feat is a species pick, so it sits in the `Species` details (D22);
  the homebrew background is fully specified inline (D21). The six Warlock spells are known only
  as the current prepared set, not by the level each was picked, so they sit unplaced in the
  header — the mixed form (D4). Skills chosen at Fighter 1 are not recorded on the compiled sheet
  and are omitted. Whether Spellfire Spark or Fey Sentinel grant an ability bonus is unverified
  (⚑ Francesco): if they do, the bonus goes first in their details.
- `vice`: `Fey Pact|HB (CHA)` — the homebrew origin feat's casting ability, as the sheet states it.
  Planned levels 3–5 are blocks above `Classes: Warlock 2` (D20).
